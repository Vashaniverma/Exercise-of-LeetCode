class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        if(len(s)!=len(t)):
            return False
        s=sorted(s)
        print(s)
        t=sorted(t)
        print(t)
        if(s==t):
            return True
        else:
            return False
        
