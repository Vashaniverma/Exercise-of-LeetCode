class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        n=len(nums)
        m=len(set(nums))
        if(m==n):
            return False
        else:
            return True
